.span6.offset2.well
  %legend= "Redeem for #{current_merchant.restaurants.first.name}"
  = render 'devise/shared/flash', flash: flash
  = form_for @redemption, url: validate_redemptions_path do |f|
    = f.text_field :code, class: 'span6', placeholder: 'Repemption Code'
    %button{ type: 'submit', class: 'btn btn-info btn-block', name: 'submit'} Redeem Now
